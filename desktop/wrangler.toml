# Static Vite SPA only. Business APIs and authentication verification live in
# courseboard-api; this Worker only serves the shared React/Tauri assets.
name = "courseboard"
compatibility_date = "2026-07-20"

[assets]
directory = "dist"
html_handling = "auto-trailing-slash"
not_found_handling = "single-page-application"
