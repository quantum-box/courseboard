mutation createClientForClientsList($input: ClientInput!) {
	createClient(input: $input) {
		id
		name
		email
		phoneNumber
		industry
		capital
		headOfficeAddress {
			state
			city
			address1
		}
	}
}
