# 日別予約状況の取り込み（撤去済み）

Linear: PLT-3247（仕様確定）/ PLT-3248（取り込み）/ PLT-3249（冪等性）/ PLT-3250（プレビューとエラー表示）

## この作業は取り下げた

同じ帳票を取り込む実装が2つ並行して作られ、両方が main に入った。

- この taskdoc の実装（#198、#222）— CourseBoard の DB に `golf_reservation_day_summaries`
  と `golf_reservation_course_links` を作り、xlsx を CourseBoard 側の calamine で解析した。
- [reservation-report-import](../../v0.1.10/reservation-report-import/task.md)（#212）—
  Field の汎用 tabular analyze に CSV / Excel / PDF を解析させ、集計を Field の
  extension config に置いた。

**残したのは後者。** [ADR-0007](../../../../architecture/decisions/ADR-0007-external-reservation-report-snapshots.md)
が保存先を決めており、その Alternatives Considered は前者の形（CourseBoard に専用テーブル）を
「ADR-0005 の物理移送禁止に反する」として明示的に不採用にしている。

前者の撤去が `202608140001_drop_golf_reservation_summary_tables.sql`。

## 残したほうが優れていた点

判断の材料として、比較で分かったことを残す。

- **付け替え・除外時の古い行**: #212 は行を `sourceCourseKey`（帳票上の施設名）で引き、
  有効な全コースを走査して掃除する。こちらは同じ結論に辿り着くまでに数回の修正を要した。
- **複数の施設名が1コースに対応**: #212 は対応付けの検証で reject する。こちらは合算して
  警告を出す形にしたが、reject のほうが単純で、silent な取りこぼしも起きない。

移植すべきものは無かった。

## 実ファイルで検証済み（2026-08-15）

`日別予約状況_組数_20260718_202607_真駒内_滝の_羊ケ丘.xlsx` を #212 の
`parse_reservation_report` に通し、シートの生セルと突き合わせた。**すべて一致した。**

Field への到達は不要だった。#212 は既知のレイアウトを CourseBoard 側の calamine で
解析し、Field の tabular analyze はそれ以外の形式のフォールバックとして使う。

| 確認項目 | シートの生セル | #212 |
|---|---|---|
| 真駒内 7/1 午前 | 組数 70 / キャ付 21 | 一致 |
| 全体 合計 | 6314 / 2476 | 一致 |
| 施設の和 | 2249 + 2399 + 1666 = 6314 | 一致 |
| 真駒内の休場日 7/6・7/7・7/9〜7/12 | 整数 `0`（空欄ではない。同日も滝の・羊ケ丘は営業） | 0 として取り込む |
| 行数 | 3施設 × 31日 × 2 = 186 | 186 |
| `全体` 行 | — | 除外済み |
| 日付セルの天気 `7/1(水)/晴→晴` | あり | 解析成功 |
| 予算行の全角ハイフン `－` | あり | エラーにならず |
| `36H` / `27H` / `18H` の接尾 | あり | key から除去 |

**この taskdoc は当初、真駒内 7/1 午前を「組数 56」と書いていた。誤り。**
`56` は**全体**の 7/1 午前の**キャディ付き**組数（シート行3・列G）であって、真駒内の
組数（行20・列F = 70）ではない。ADR-0006 が挙げる「7月1日の午前56組・午後15組」も
全体のキャディ付き組数を指しており、そちらは正しい。

ファイル内に年が無いので、年は外から与える必要がある — これは #212 も取り込み画面で
対象年を必須選択させている。

パーサはレイアウトに対して意図的に厳格（変わった帳票は推測せず拒否する）なので、
この実ファイルを fixture として `tests/fixtures/` に戻し、#212 の
`parse_reservation_report` に対する回帰テストを
`src/course/usecase/reservation_report_import.rs` に足した。上の表の各項目
（施設数・行数・施設ごとの合計・真駒内 7/1 午前・休場日の 0・`全体` の除外）を
そのまま assert している。fixture は本 taskdoc の実装が一度リポジトリに置いて
いたものと同じファイル（sha256 `6c86ff72…`）。

## 元の前提（記録として）

札幌カントリー倶楽部の予約は ICグリーンシステムにある。そこから出せるのは日次サマリの
ファイル1つだけで、予約明細は出せない（2026-08-09 先方確認済み）。スタート時刻も、
予約1件ごとのキャディ有無も出ない。したがって Field の予約在庫には落とせない —
枠に紐づく事実が1つも無く、枠を消費させようとすると存在しない明細を捏造することになる。

この前提自体は ADR-0007 でも変わっていない。変わったのは「ではどこに置くか」の答えで、
CourseBoard の DB ではなく Field の運用スナップショットになった。
