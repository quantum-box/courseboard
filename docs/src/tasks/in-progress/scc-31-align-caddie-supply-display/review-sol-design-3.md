# SCC-31 詳細設計 限定確認

## 結論

**NO-GO**

新 action の実装指示は design.md から除去され、group 処理順も安定化されている。
しかし task.md のテストアウトラインに旧スコープの「受付ロールの認可」が残り、
確定事項8および design.md の「既存読取可能ロールの非後退」テストと矛盾する。

## 確認項目

| 確認項目 | 判定 | 確認結果 |
| --- | --- | --- |
| 1. task.md と design.md の整合 | 不十分 | 認可方針はともに「既存 `ListCaddieInsights` を維持し、受付公開は後続タスク」で一致する（`task.md:96-106,122-126`、`design.md:65-71,97-99`）。一方、task.md のテストアウトラインだけが `受付ロールの認可` を検証対象に残しており（`task.md:136-141`）、design.md の既存ロール非後退テスト（`design.md:123-128,130-137`）と一致しない。 |
| 2-a. 認可テストの置換 | 不十分 | design.md は「既存 `ListCaddieInsights` 保有ロールが 403 に後退しない」に正しく置換済みだが、task.md の旧記述が未置換である。 |
| 2-b. 削除した action の残骸 | 解消 | design.md の実装対象・PR・テスト計画に `ListCourseCaddieSupply`、`LIST_COURSE_CADDIE_SUPPLY`、manifest/policy 更新の指示は残っていない。`actions.rs` / `READ_ONLY` / `ALL` / manifest を変更しない旨（`design.md:69`）とレビュー対応記録の履歴参照（`design.md:235,238-239`）は、削除方針を明示するための記録であり残骸ではない。 |
| 2-c. group 処理順 | 解消 | tee time 昇順、同時刻は reservation ID、group 内は caddie ID / assignment ID の安定順が維持され、capacity 競合テストも追加されている（`design.md:21-25,123`）。 |
| 2-d. 新たな重大・高相当の欠落 | 高1件 | 上記の認可テスト範囲の矛盾以外に、今回のスコープ編集に起因する重大・高相当の欠落は確認しなかった。 |
| 3. 実装エージェントへの引き渡し | NO-GO | task.md の受け入れテスト範囲が一意になるまで渡さない。 |

## 必須修正

`task.md:139` の「受付ロールの認可」を、確定事項8に合わせて
「既存 `ListCaddieInsights` 保有ロールが supply を引き続き読め、403 に後退しないこと」へ置換する。

あわせて誤読防止のため、`design.md:67` の「既存 ... 要求のまま変更し」は
「既存 ... 要求のまま変更せず」へ直すことを推奨する。後続段落は変更しない方針を明記しているため、
こちらは単独では NO-GO 理由に数えない。

## 再判定

**GO**

- `task.md:139-140` は、既存 `ListCaddieInsights` 保有ロールが supply を引き続き読めて
  403 に後退しないことの検証へ置換され、確定事項8および `design.md:127,137` と整合した。
- `design.md:67` は「既存 ... 要求のまま変更せず」へ修正され、後続タスクへ切り出す方針が
  一意に読める。

初回 NO-GO の必須修正は解消した。この design.md を実装エージェントへ引き渡してよい。
