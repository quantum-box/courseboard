apiVersion: apps.tachy.one/v1alpha
kind: OAuth2Client
metadata:
  name: courseboard-local-prod-pkce
  tenantId: tn_01ks18jhh1xvggktfzjx5jqsen
spec:
  # Public client for the platform-ui style React password login + JSON PKCE.
  # courseboard-api validates the resulting api.n1.tachy.one issuer and this
  # client id as its expected audience.
  clientType: public
  redirectUris:
    - http://127.0.0.1:5173/oauth/callback
    # 本番web（courseboard.txcloud.app）のクライアントサイドCognito PKCE。
    # SPAはlocation originから/oauth/callbackを導出する（adapters.ts）。
    - https://courseboard.txcloud.app/oauth/callback
  allowedScopes:
    - openid
    - profile
    - email
  grantTypes:
    - authorization_code
    - refresh_token
  useTachyonUserPool: true
